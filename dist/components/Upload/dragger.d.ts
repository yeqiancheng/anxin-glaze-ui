import React from "react";
interface DraggerProps {
    onFile: (files: FileList) => void;
    children?: React.ReactNode;
}
export default function Dragger(props: DraggerProps): import("react/jsx-runtime").JSX.Element;
export {};
